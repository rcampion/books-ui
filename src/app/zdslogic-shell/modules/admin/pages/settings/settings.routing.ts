import { Route } from '@angular/router';
import { SettingsComponent } from 'app/zdslogic-shell/modules/admin/pages/settings/settings.component';

export const settingsRoutes: Route[] = [
    {
        path     : '',
        component: SettingsComponent
    }
];
